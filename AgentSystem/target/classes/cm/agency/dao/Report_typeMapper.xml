<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="cm.agency.dao.Report_typeMapper">
   <select id="Select_Report" resultType="As_accountdetailEntity">
     SELECT a.id,u.userName,a.accountMoney from as_accountdetail as a ,as_user as u WHERE a.userId=u.id
   </select>

   </mapper>
    